# Preparations

For leptos hydrate rendering (SSR+CSR), we need the following preparations:

```console
cargo binstall cargo-leptos
cargo binstall cargo-generate
rustup target add wasm32-unknown-unknown
```
